# All values below are optional in development — DevelopmentSettings
# already provides sensible defaults. Override here when you need to.

# ENVIRONMENT=development            # development | production | test
# LOG_LEVEL=INFO

# DATABASE_URL=...                   # see src/core/config/dev.py for the dev default
# DATABASE_ECHO=false

# CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
